# Valor GitHub Integration — Phased Implementation Plan

**Status:** Draft, pending approval
**Owner:** TBD (defaults to project lead)
**Target customer:** Remedy Reconstruction (Valor's ConstructionOS vertical)
**Target ship:** First PR review comment posted by Valor Bot on a Remedy repo

## 1. Context

Valor needs to integrate with GitHub as a full member of its customers'
engineering workflow, not as an afterthought. Remedy Reconstruction, the
first production customer of Valor's ConstructionOS vertical, runs their
own GitHub organization and expects Valor to participate in their pull
request review cycle, CI gating, and eventually their deployment
automation.

This plan describes the build-out of that integration as a standalone
Valor subsystem — the **Valor GitHub App** — and its first concrete
workflow, a **PR review bot**. The first bot posts a structured code
review comment whenever a pull request is opened on a Remedy repo,
generated by Valor's existing code review agents.

The plan is phased so that work can run in parallel across multiple
humans and AI subagents, coordinated through the repository's
`WORKLOG.md` and standard git conventions. It also incorporates
**explicit dogfood lessons** from the Monty-CNS setup session (see
section 5) — specific failure modes we've lived through and are
determined not to repeat.

Foundational reference docs:

- `docs/security/github-auth.md` — the authentication deep-dive (why
  GitHub App, JWT → installation token flow, GitHubKit SDK, rate
  limits, private key storage)
- `docs/security/actor-model.md` — why the Valor Bot is a service/bot
  actor, not an AI agent or a human, and what that means for its
  permissions and audit trail
- `docs/security/threat-model.md`, `classification.md`,
  `compromise-playbook.md` — Monty-CNS's security posture, extended
  here to cover the Valor Bot's production obligations
- `docs/plans/README.md` — the plans directory conventions,
  coordination rules, and commit format all plans follow

## 2. Success criteria

This plan ships when all of the following are measurably true:

1. **Functional end-to-end.** A PR opened on a Remedy Reconstruction
   repo receives a Valor-Bot-authored review comment within 2 minutes
   of opening, on 99% of attempts over a 7-day rolling window.
2. **Secure by default.** The Valor App's private key is in the
   self-hosted Vault, never on a developer laptop, never in a git
   repo, rotated on a documented schedule.
3. **Observable.** A Grafana dashboard shows webhook delivery rate,
   signature verification failures, installation token refresh rate,
   comment-post latency (p50/p95/p99), and rate limit headroom. Alerts
   fire within 2 minutes on any of: webhook 5xx rate > 1%,
   installation token failures, stale webhook deliveries > 1 minute.
4. **Idempotent.** Duplicate webhook deliveries (GitHub retries) are
   detected and no-oped. Comment posts are idempotent (second delivery
   for the same commit does not post a duplicate comment — it edits
   the existing one).
5. **Cross-platform tested.** CI matrix runs on Linux and Windows
   (matching Valor's existing `ship-gate.yml` pattern). Both pass.
6. **Dogfood regression.** The six named regression tests from section
   5 all pass — specific failure modes from CNS that this system is
   explicitly defended against.
7. **Runbook in place.** `docs/plans/valor-github-integration-runbook.md`
   exists with: deploy procedure, rollback procedure, key rotation
   procedure, incident playbook, and on-call escalation. Tested by a
   practice drill where we rotate the private key without downtime.
8. **Reviewed by security.** Either the plan or the shipped system has
   been reviewed by a qualified security / compliance professional,
   with findings addressed before production rollout. (This is
   especially important because Valor will eventually touch
   Restricted-class data under SOX/GLBA/FINRA frameworks — see
   `docs/security/valor-scope.md`.)

## 3. Non-goals

What this plan **explicitly does not** cover. Each is a valid future
phase but would blow up the scope of the first ship:

- **CI deployment automation.** The bot does not deploy code. It reads
  PRs, generates reviews, posts comments. Deploy automation is a
  separate phase, phase 10+.
- **Issue-to-project synchronization.** Two-way sync between GitHub
  Issues and Valor's internal project tracking is a valid workflow but
  not this ship.
- **OAuth App for human end-users.** The bot doesn't authenticate
  individual Remedy employees who visit a Valor dashboard. That's an
  OAuth App pattern and a separate system.
- **Modification of Valor's existing `core/auth.py`.** The GitHub
  integration is purely additive. A new `core/github_auth.py` sibling
  module holds the JWT/installation-token logic; the existing
  `VALOR_API_KEY` inter-agent auth is not touched.
- **Replacement of Valor's existing CI.** The existing
  `.github/workflows/ship-gate.yml` is extended, not rewritten. A new
  job `test-github-integration` is added alongside the existing
  Windows and Linux matrices.
- **Migration of Valor's other OAuth integrations.** Valor already has
  `configs/intuit_oauth.json` (QuickBooks OAuth). This plan borrows
  the pattern but does not re-implement or migrate the existing
  Intuit integration.
- **A generic "GitHub Apps framework" for Valor.** We build exactly
  what's needed for the PR review bot. Generalization happens in a
  later refactor after we've shipped two or three concrete workflows
  and can see the real abstraction.
- **Multi-tenancy beyond Remedy.** The first shipped version runs
  against one customer (Remedy). Multi-customer installation
  management is a later phase.

## 4. Architecture

### 4.1 System diagram

```
                ┌──────────────────────────────────────────────┐
                │            github.com                        │
                │  (Remedy Reconstruction organization)        │
                │                                              │
                │  App: valor-bot                              │
                │  ├── Private key (PEM) — held by Remedy      │
                │  ├── Webhook URL → Valor tailnet endpoint    │
                │  ├── Webhook secret (HMAC key)               │
                │  └── Permissions: Contents:R, PRs:R/W,       │
                │      Metadata:R                              │
                │                                              │
                │  Installation: Remedy's repos                │
                │  - remedy/cons-os                            │
                │  - remedy/field-app                          │
                │  - (others as they onboard)                  │
                └───────┬──────────────────────────────────────┘
                        │
                   webhook POST
                   (HMAC-signed)
                        │
                        ▼
          ┌────────────────────────────────────┐
          │  Tailscale tunnel                  │
          │  webhook.valor.tail-xxxx.ts.net    │
          │  → rack-hosted Valor production    │
          └──────────┬─────────────────────────┘
                     │
                     ▼
   ┌─────────────────────────────────────────────────┐
   │  Valor production (self-hosted rack)            │
   │                                                 │
   │  FastAPI app (extends existing core/)           │
   │  ├── POST /gh-hook                              │
   │  │   1. verify HMAC sig (webhooks.hmac)         │
   │  │   2. dedupe by X-GitHub-Delivery (Redis)     │
   │  │   3. enqueue Celery task                     │
   │  │   4. return 200 in <500ms                    │
   │  │                                              │
   │  └── Celery worker                              │
   │      1. fetch installation token (cached)      │
   │      2. call GitHubKit client to get PR diff    │
   │      3. dispatch to Valor code review agent     │
   │         (via MontyCore /ask endpoint)           │
   │      4. format comment as markdown              │
   │      5. post comment via GitHubKit              │
   │      6. record audit log entry                  │
   │                                                 │
   │  Supporting:                                    │
   │  - HashiCorp Vault (holds App private key)      │
   │  - Redis (token cache, dedupe, Celery broker)   │
   │  - Prometheus (metrics)                         │
   │  - Grafana (dashboards)                         │
   │  - Loki or file logs (structured JSON)          │
   └─────────────────────────────────────────────────┘
                     │
           GitHub API calls
           (installation token)
                     │
                     ▼
                github.com API
```

### 4.2 New modules (all additive)

```
valor2.0/
├── core/
│   ├── auth.py               (existing — NOT touched)
│   ├── authz.py              (existing — NOT touched)
│   ├── config.py             (existing — extend with GITHUB_* vars)
│   ├── approval_token_store.py (existing — reference pattern for…)
│   ├── github_auth.py        (NEW — JWT + installation token logic)
│   ├── github_token_cache.py (NEW — Redis-backed installation token cache)
│   └── github_client.py      (NEW — GitHubKit wrapper w/ retries + metrics)
│
├── webhooks/                 (NEW directory)
│   ├── __init__.py
│   ├── github/
│   │   ├── __init__.py
│   │   ├── router.py         (FastAPI router: POST /gh-hook)
│   │   ├── signature.py      (HMAC verification, pure function)
│   │   ├── dedupe.py         (Redis-backed idempotency)
│   │   └── events.py         (event type → handler dispatch)
│
├── agents/                   (existing directory — add one new agent)
│   └── github_pr_reviewer.py (NEW agent, follows Valor V2 envelope)
│
├── tasks/                    (Celery tasks)
│   └── github_pr_review.py   (NEW — the async PR review pipeline)
│
├── tests/
│   ├── conftest.py           (existing — extend with GitHub fixtures)
│   ├── smoke_github_auth.py        (NEW — smoke-style standalone script)
│   ├── smoke_github_webhook.py     (NEW)
│   ├── smoke_github_pr_review.py   (NEW — full E2E smoke)
│   ├── unit/
│   │   └── github/                 (NEW unit test dir, pytest)
│   │       ├── test_signature.py
│   │       ├── test_dedupe.py
│   │       ├── test_github_auth.py
│   │       ├── test_github_client.py
│   │       └── test_pr_reviewer.py
│   └── integration/
│       └── github/                 (NEW — real GitHub calls with test App)
│           ├── test_auth_flow.py
│           ├── test_webhook_roundtrip.py
│           └── test_pr_review_e2e.py
│
├── configs/
│   ├── intuit_oauth.json     (existing — pattern reference only)
│   └── github_app.json       (NEW — metadata; never contains secrets)
│
├── docs/
│   └── plans/
│       ├── valor-github-integration.md           (this plan)
│       ├── valor-github-integration-runbook.md   (NEW — operations)
│       └── worklogs/
│           └── valor-github-integration.md       (NEW — claim log)
│
└── .github/
    └── workflows/
        └── ship-gate.yml     (existing — extend with test-github-integration job)
```

**Key principle:** every new file is additive. No existing file is
replaced, only extended. Any modification to an existing file (only
`core/config.py`, `tests/conftest.py`, and `.github/workflows/ship-gate.yml`)
is strictly additive — new constants, new fixtures, new jobs — with
zero touch to existing functionality.

### 4.3 Request flow for a single PR review

1. **Human opens a PR** on `remedy/cons-os`
2. GitHub fires `pull_request` webhook with action=`opened`
3. **POST** arrives at `webhook.valor.tail-xxxx.ts.net/gh-hook` over Tailscale
4. FastAPI router (`webhooks/github/router.py`) reads the raw body
5. **HMAC verification** (`signature.verify`) — returns 401 if bad signature, constant-time comparison
6. **Dedupe check** (`dedupe.already_processed(delivery_id)`) — returns 200 if already seen
7. **Event routing** (`events.dispatch(event_type, payload)`) — routes `pull_request.opened` to the PR reviewer handler
8. Handler enqueues a Celery task `github_pr_review(delivery_id, payload)` and returns 200
9. Celery worker picks up the task
10. Worker calls `get_installation_token(app_id, installation_id)` → cache hit or fresh mint
11. Worker uses `github_client.get_pull_request(owner, repo, number)` to fetch full PR metadata + diff
12. Worker dispatches to Valor's code review agent via MontyCore `/ask`:
    ```json
    {
      "command": "review_pr",
      "data": {"diff": "...", "files": [...], "title": "...", "body": "..."},
      "session_id": "gh-delivery-<id>"
    }
    ```
13. Agent returns a structured review in V2 envelope format
14. Worker formats the review as markdown (template in `tasks/github_pr_review.py`)
15. Worker posts comment via `github_client.post_pr_comment()`
16. Worker records an audit log entry (structured JSON, stored + emitted as metric)
17. **Second delivery for the same commit** (GitHub retry or re-push) → dedupe step returns 200 early, OR if new commit, worker detects existing comment and **edits** it instead of posting duplicate

### 4.4 Trust boundaries

Three trust boundaries, each with its own verification:

- **GitHub → Valor webhook:** HMAC signature on every delivery.
  Constant-time comparison. Body read raw before any JSON parsing
  (so re-serialization doesn't break the signature).
- **Valor → GitHub API:** installation token (1-hour TTL) minted from
  a JWT signed with the App private key. Private key lives in Vault,
  fetched once at worker startup, held in process memory only. Tokens
  cached in Redis with TTL, never written to disk.
- **Webhook handler → Celery worker:** internal Valor trust boundary.
  The handler enqueues tasks; the worker trusts the task payload.
  Payload validation happens at enqueue time (pydantic models), so
  malformed deliveries are rejected before they become tasks.

Anything the handler can't verify is rejected at 401 before touching
the dedupe cache or the task queue. **Untrusted data never reaches the
Celery worker.**

### 4.5 Where each piece of Valor's existing infrastructure plugs in

| Existing Valor capability | Used by this plan |
|---|---|
| `core/auth.py` (VALOR_API_KEY, `check_api_key`) | Inbound endpoints for internal Valor calls (unchanged) |
| `core/config.py` (`os.getenv` pattern) | Extended with `GITHUB_APP_*` variables |
| `core/approval_token_store.py` | Pattern reference for `core/github_token_cache.py` |
| MontyCore `/ask` endpoint | How the PR reviewer task talks to the code review agent |
| V2 envelope format (`{status, data, error}`) | All new handlers return V2 envelopes |
| Celery + Redis (existing broker) | New `github_pr_review` task plugs into existing worker pool |
| `tests/conftest.py` (socket reachability fixtures) | Extended with GitHub mock fixtures |
| `tests/smoke_*.py` convention | New `smoke_github_*.py` scripts follow the same standalone-script pattern |
| `.github/workflows/ship-gate.yml` cross-platform matrix | New `test-github-integration` job added alongside |
| `configs/intuit_oauth.json` (Intuit OAuth pattern) | Pattern reference for App config shape |
| HashiCorp Vault on the rack (self-hosting.md) | Production home for the App private key |
| Tailscale (self-hosting.md) | Production fronting for the webhook URL |

The plan is **mostly glue** on top of existing patterns. That's deliberate — the less new infrastructure we introduce, the less novel failure surface we own.
