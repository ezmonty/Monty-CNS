# Security Notes

This folder exists for policy, not for storing the hidden lane itself.

The actual hidden lane should remain outside the main vault unless there is a deliberate reason to do otherwise.
Use this folder for:
- security policy
- redaction rules
- export hygiene
- storage decisions
- threat assumptions
