{
  "name": "memory",
  "description": "Persistent structured memory across sessions — entities, relations, and observations stored in a local JSON knowledge graph. No secrets needed.",
  "scope": "user",
  "upstream": "https://github.com/modelcontextprotocol/servers/tree/main/src/memory",
  "install": {
    "command": "npm",
    "args": ["install", "-g", "@modelcontextprotocol/server-memory"]
  },
  "config": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-memory"]
  },
  "secrets_required": [],
  "notes": [
    "Data is stored in a local JSON file — no external service or API key required.",
    "Useful for maintaining context between sessions: project decisions, user preferences, recurring facts.",
    "The knowledge graph stores entities (nodes), relations (edges), and observations (properties on entities)."
  ]
}
